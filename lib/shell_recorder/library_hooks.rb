# frozen_string_literal: true

module ShellRecorder
  # LibraryHooks holds hooks into different libraries, when any of those classes
  # are required, they will configure themselves to hook into the given library.
  module LibraryHooks
  end
end
